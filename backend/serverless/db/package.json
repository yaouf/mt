{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && tsc",
    "knex": "npm i && npx ts-node node_modules/.bin/knex",
    "update": "npm i && npm run knex -- migrate:rollback && npm run knex -- migrate:latest && npm run knex -- seed:run && tsc",
    "new": "npm i && npx knex migrate:make $1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^3.1.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.26"
  }
}

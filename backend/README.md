# Backend

## Instructions for Backend Development

1. Download recommended workspace extensions.
2. Open command prompt using `Ctrl + ~` and run `npm run dev` to install dependencies, start the server, and watch for changes.
3. Use Source Control to commit changes and push to GitHub.
# Frontend

`npm run start`

`src/pages` holds the main pages for the app: Home, Login, Search, Sections, and Settings (for now)
`src/components` is a folder for smaller components that will be reused, such as Articles

the `<BDH/>` component puts all of the screens together in the navbar and this component is rendered inside of `App.js`
